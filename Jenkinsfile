pipeline {
    agent any
    stages{
        stage("build"){
            steps{
                sh "java --version"
            }
        }

        stage("QA"){
            steps{
                sh "echo hola QA mundo 2"
            }
        }   
    }
}